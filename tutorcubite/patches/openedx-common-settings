# Cubite brand: platform-wide naming and contacts (LMS + Studio)
PLATFORM_NAME = "{{ CUBITE_PLATFORM_NAME }}"
PLATFORM_DESCRIPTION = "{{ CUBITE_PLATFORM_DESCRIPTION }}"
TECH_SUPPORT_EMAIL = "{{ CUBITE_SUPPORT_EMAIL }}"
CONTACT_EMAIL = "{{ CUBITE_CONTACT_EMAIL }}"
DEFAULT_FROM_EMAIL = "{{ CUBITE_CONTACT_EMAIL }}"
DEFAULT_FEEDBACK_EMAIL = "{{ CUBITE_SUPPORT_EMAIL }}"
SERVER_EMAIL = "{{ CUBITE_CONTACT_EMAIL }}"
BUGS_EMAIL = "{{ CUBITE_SUPPORT_EMAIL }}"
FAVICON_PATH = "{{ CUBITE_FAVICON_PATH }}"
